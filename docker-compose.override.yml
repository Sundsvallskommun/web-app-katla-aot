# Local-only host ports. Deployment platforms route to the exposed container
# ports from docker-compose.yml and should load that file without this override.
services:
  backend:
    ports:
      - '${BACKEND_PORT-3001}:3000'

  frontend:
    ports:
      - '${FRONTEND_PORT-3000}:3000'
